{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "definitions": {
        "keys::X": {
            "type": "object",
            "properties": {"a": {"type": "string"}, "b": {"type": "boolean"}},
            "required": ["a", "b"]
        },
        "keys::Y": {"enum": ["a", "b"]}
    }
}
